"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = zipLongest;

var _iter = _interopRequireDefault(require("./iter"));

function* zipLongest(filler, ...args) {
  const iters = args.map(x => (0, _iter.default)(x));

  while (true) {
    let numberOfExausted = 0;
    const zipped = [];

    for (const iter of iters) {
      const {
        done,
        value
      } = iter.next();

      if (done) {
        numberOfExausted++;
      }

      zipped.push(done ? filler : value);
    }

    if (iters.length === numberOfExausted) {
      return;
    }

    yield zipped;
  }
}

module.exports = exports["default"];
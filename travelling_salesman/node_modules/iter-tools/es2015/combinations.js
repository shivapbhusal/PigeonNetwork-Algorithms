"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = combinations;

var _from = _interopRequireDefault(require("@babel/runtime/core-js/array/from"));

var _map = _interopRequireDefault(require("./map"));

var _range = _interopRequireDefault(require("./range"));

var _permutations = _interopRequireDefault(require("./permutations"));

var _iter = _interopRequireDefault(require("./iter"));

function isSorted(arr) {
  if (arr.length < 2) return true;

  for (let i = 1; i < arr.length; i++) {
    if (arr[i - 1] > arr[i]) {
      return false;
    }
  }

  return true;
}

function* combinations(iterable, r) {
  const arr = (0, _from.default)((0, _iter.default)(iterable));
  const mapToIndex = (0, _map.default)(function (i) {
    return arr[i];
  });
  const n = arr.length;

  for (let indices of (0, _permutations.default)((0, _range.default)(n), r)) {
    if (isSorted(indices)) {
      yield (0, _from.default)(mapToIndex(indices));
    }
  }
}

module.exports = exports["default"];
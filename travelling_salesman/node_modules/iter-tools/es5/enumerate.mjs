import range from './range';
import zip from './zip';
export default function enumerate(iterable, start) {
  if (start === void 0) {
    start = 0;
  }

  return zip(range({
    start: start
  }), iterable);
}
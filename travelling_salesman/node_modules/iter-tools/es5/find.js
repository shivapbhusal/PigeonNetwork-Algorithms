"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

exports.__esModule = true;
exports.default = curriedFind;

var _getIterator2 = _interopRequireDefault(require("@babel/runtime/core-js/get-iterator"));

var _iter = _interopRequireDefault(require("./iter"));

function find(func, iterable) {
  var found = true;
  var c = 0;

  for (var _iterator = (0, _iter.default)(iterable), _isArray = Array.isArray(_iterator), _i = 0, _iterator = _isArray ? _iterator : (0, _getIterator2.default)(_iterator);;) {
    var _ref;

    if (_isArray) {
      if (_i >= _iterator.length) break;
      _ref = _iterator[_i++];
    } else {
      _i = _iterator.next();
      if (_i.done) break;
      _ref = _i.value;
    }

    var item = _ref;
    found = func(item, c++);

    if (found) {
      return item;
    }
  }

  return null;
}

function curriedFind(func, iterable) {
  if (!iterable) {
    return function (iterable) {
      return find(func, iterable);
    };
  }

  return find(func, iterable);
}

module.exports = exports["default"];
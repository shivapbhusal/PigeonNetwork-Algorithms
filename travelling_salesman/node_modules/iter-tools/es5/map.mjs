import _regeneratorRuntime from "@babel/runtime/regenerator";
import _getIterator from "@babel/runtime/core-js/get-iterator";

var _marked =
/*#__PURE__*/
_regeneratorRuntime.mark(map);

import iter from './iter';

function map(func, iterable) {
  var c, _iterator, _isArray, _i, _ref, item;

  return _regeneratorRuntime.wrap(function map$(_context) {
    while (1) {
      switch (_context.prev = _context.next) {
        case 0:
          c = 0;
          _iterator = iter(iterable), _isArray = Array.isArray(_iterator), _i = 0, _iterator = _isArray ? _iterator : _getIterator(_iterator);

        case 2:
          if (!_isArray) {
            _context.next = 8;
            break;
          }

          if (!(_i >= _iterator.length)) {
            _context.next = 5;
            break;
          }

          return _context.abrupt("break", 17);

        case 5:
          _ref = _iterator[_i++];
          _context.next = 12;
          break;

        case 8:
          _i = _iterator.next();

          if (!_i.done) {
            _context.next = 11;
            break;
          }

          return _context.abrupt("break", 17);

        case 11:
          _ref = _i.value;

        case 12:
          item = _ref;
          _context.next = 15;
          return func(item, c++);

        case 15:
          _context.next = 2;
          break;

        case 17:
        case "end":
          return _context.stop();
      }
    }
  }, _marked, this);
}

export default function curriedMap(func, iterable) {
  if (!iterable) {
    return function (iterable) {
      return map(func, iterable);
    };
  }

  return map(func, iterable);
}
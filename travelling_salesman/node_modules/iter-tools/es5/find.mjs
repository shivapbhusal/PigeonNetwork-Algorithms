import _getIterator from "@babel/runtime/core-js/get-iterator";
import iter from './iter';

function find(func, iterable) {
  var found = true;
  var c = 0;

  for (var _iterator = iter(iterable), _isArray = Array.isArray(_iterator), _i = 0, _iterator = _isArray ? _iterator : _getIterator(_iterator);;) {
    var _ref;

    if (_isArray) {
      if (_i >= _iterator.length) break;
      _ref = _iterator[_i++];
    } else {
      _i = _iterator.next();
      if (_i.done) break;
      _ref = _i.value;
    }

    var item = _ref;
    found = func(item, c++);

    if (found) {
      return item;
    }
  }

  return null;
}

export default function curriedFind(func, iterable) {
  if (!iterable) {
    return function (iterable) {
      return find(func, iterable);
    };
  }

  return find(func, iterable);
}